// window.addEventListener('scroll', function() {
//     const images = document.querySelectorAll('img');
//     const scrollY = window.scrollY;
    
//     images.forEach(img => {
//         const blur = scrollY / 20;
//         img.style.filter = `blur(${blur}px)`;
//     });
// });